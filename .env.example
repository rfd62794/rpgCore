# APJ Agent Swarm — Environment Configuration
# Copy to .env and fill in values

# ── Local Ollama (default, no cost) ──────────────────────────
OLLAMA_BASE_URL=http://localhost:11434

# ── OpenRouter (Director — use sparingly) ────────────────────
# Get your key at https://openrouter.ai/keys
# FREE tier available — DeepSeek R1, Llama 3.1 70B, Gemini Flash
OPENROUTER_API_KEY=your_key_here

# Director model — the Generalist
# Recommended free options:
#   deepseek/deepseek-r1               (strong reasoning, free tier)
#   meta-llama/llama-3.1-70b-instruct  (strong generalist, free tier)
#   google/gemini-flash-1.5            (large context, free tier)
OPENROUTER_MODEL=deepseek/deepseek-r1

# ── Approval Gates ───────────────────────────────────────────
# Controls when the Director can be called
# STRICT   = explicit human approval required every call (default)
# MODERATE = approval required for new task types, auto for known
# OFF      = Director disabled, local only
DIRECTOR_APPROVAL_MODE=STRICT
